---
title: '函数节流'
date: 2022-01-10
tag:
  - javascript专题系列
categories:
  - javascript专题系列
---

```js
const throttle = function (func, wait) {
  let timer = null

  return function () {
    let context = this
    let args = arguments

    if (!timer) {
      timer = setTimeout(() => {
        func.apply(context, args)
        clearTimeout(timer)
        timer = null
      }, wait)
    }
  }
}
```
